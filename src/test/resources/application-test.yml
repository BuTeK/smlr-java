spring:
  jmx:
    enabled: false
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  datasource:
    driverClassName: org.h2.Driver
    url: "jdbc:h2:mem:smlr;\
                        MODE=PostgreSQL;\
                        INIT=CREATE SCHEMA IF NOT EXISTS smlr\\;SET SCHEMA smlr;\
                        DB_CLOSE_DELAY=-1;\
                        DB_CLOSE_ON_EXIT=FALSE"
    username: sa
    password: